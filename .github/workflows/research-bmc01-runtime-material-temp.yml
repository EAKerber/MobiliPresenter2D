name: Research BMC01 runtime material gate temp
# retry after async render revision gate

on:
  push:
    branches:
      - research/reconstruction-architecture-v0.1
    paths:
      - .github/workflows/research-bmc01-runtime-material-temp.yml

permissions:
  contents: write

jobs:
  browser:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research/reconstruction-architecture-v0.1
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Syntax smoke
        run: |
          node --check app/core/reconstruction.js
          node --check app/data/reconstruction-data.js
          node --check app/app.js
      - name: Install isolated browser tooling
        run: |
          npm install --prefix /tmp/bmc01-browser --no-audit --no-fund playwright@1.62.1
          /tmp/bmc01-browser/node_modules/.bin/playwright install --with-deps chromium
      - name: Run query-gated reconstruction browser contract
        env:
          NODE_PATH: /tmp/bmc01-browser/node_modules
          BMC01_BROWSER_URL: http://127.0.0.1:4173/app/index.html
        run: |
          set -euo pipefail
          python -m http.server 4173 --directory . >/tmp/bmc01-http.log 2>&1 &
          server_pid=$!
          trap 'kill "$server_pid" 2>/dev/null || true' EXIT
          sleep 1
          node tests/reconstruction-browser.cjs /tmp/bmc01-runtime-review
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: bmc01-runtime-material-review
          path: /tmp/bmc01-runtime-review
          if-no-files-found: ignore
      - name: Self-clean with CAS
        env:
          TARGET_BRANCH: research/reconstruction-architecture-v0.1
          EXPECTED_HEAD: ${{ github.sha }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/research-bmc01-runtime-material-temp.yml
          remote_before="$(git ls-remote origin "refs/heads/$TARGET_BRANCH" | cut -f1)"
          test "$remote_before" = "$EXPECTED_HEAD"
          git commit -m "chore: self-clean BMC-01 runtime material gate"
          result="$(git rev-parse HEAD)"
          git push origin "HEAD:refs/heads/$TARGET_BRANCH"
          remote_after="$(git ls-remote origin "refs/heads/$TARGET_BRANCH" | cut -f1)"
          test "$remote_after" = "$result"
