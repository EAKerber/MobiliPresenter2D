# Baseline visual debt

Status: provisional until exact v3.3.0 bytes are imported.

The source material reports these known areas to inspect rather than silently normalize during R0:

- column definition can read too flat because its tile phase aligns with the wall;
- module 03 to module 04 contact must remain visually coherent;
- stone separation around modules 02/03 can expose seams or reconstructed areas;
- the stone region behind pans/cooktop previously showed grid-like cut artifacts and must be checked in the imported checkpoint;
- hidden-side completion for separated modules may require explicit future variants instead of naive cuts.

R0 freezes what is actually present in the exact checkpoint. It does not repair these items. Once the bytes are available, this file must be revised from observed pixels rather than historical description.
