{
  "name": "casa-em-modulos-configurador-html",
  "version": "3.3.0",
  "private": true,
  "description": "Configurador Casa em Módulos em HTML, CSS e JavaScript puro.",
  "scripts": {
    "build:stone-layers": "python3 tools/split-stone-layers.py",
    "build:inline-masks": "python3 tools/build-inline-masks.py",
    "build": "npm run build:stone-layers && npm run build:inline-masks && bash scripts/build-static.sh",
    "test": "node tools/test-core.js && python3 tools/validate-assets.py"
  }
}
