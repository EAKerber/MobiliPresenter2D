# BMC-01 — Guide-only geometry review v0.1

Status: research-only / no candidate promotion

This stage deliberately avoids deciding edit pixels.

The exact current `module-03-hidden` variant is rendered twice:

1. current runtime state, including the historical exposed-side overlay;
2. clean review state with only `module-02-right-exposed-face` removed.

No other visibility rule changes.

The clean state then receives review-only geometry overlays for:
- historical BMC-01 geometry;
- current local physical-depth-transfer geometry;
- a neutral translucent local geometry guide.

The review artifact answers a narrower question:

> does the new local geometry make sense against the exact canonical target
> before any donor or generation is allowed to change pixels?

The contact sheet is not a candidate and is never eligible for runtime
promotion.
